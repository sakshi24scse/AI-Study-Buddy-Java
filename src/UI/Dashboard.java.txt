package ui;

import javax.swing.*;

public class Dashboard extends JFrame {

    public Dashboard() {
        setTitle("AI Study Buddy - Dashboard");
        setSize(500, 400);

        JLabel label = new JLabel("Dashboard Prototype for Review 1");
        add(label);

        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);
    }
}
