package database;
import java.util.ArrayList;
import models.User;

public class UserDAO {
    private ArrayList<User> userList = new ArrayList<>();

    public void addUser(User user) {
        userList.add(user);
        System.out.println("User added to collection.");
    }

    public ArrayList<User> getAllUsers() {
        return userList;
    }
}
